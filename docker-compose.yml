services:
  mysawit-delivery:
    build: .
    container_name: mysawit-delivery
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://host.docker.internal:5435/mysawit_delivery
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:-secret}
    ports:
      - "8080:8080"